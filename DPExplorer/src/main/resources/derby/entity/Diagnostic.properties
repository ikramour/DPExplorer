CREATE=CREATE TABLE APP.DIAGNOSTIC (ID_DIAGNOSTIC BIGINT NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),CLASSE_ENERGETIQUE VARCHAR(1),CONSTRAINT DIAGNOSTIC_PK PRIMARY KEY (ID_DIAGNOSTIC))